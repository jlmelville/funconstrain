% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/problem_resolution.R
\name{funconstrain_catalog}
\alias{funconstrain_catalog}
\alias{funconstrain_problem}
\title{Discover funconstrain test problems}
\usage{
funconstrain_catalog()

funconstrain_problem(name, n = NULL, m = NULL)
}
\arguments{
\item{name}{A single exact factory name from the \code{name} column of
\code{funconstrain_catalog()}.}

\item{n}{Optional number of parameters. \code{NULL} uses the problem default.}

\item{m}{Optional number of summand functions. \code{NULL} uses the problem
default or the value derived from \code{n}.}
}
\value{
\code{funconstrain_catalog()} returns a data frame with one row per
problem and these columns:
\itemize{
\item \code{number}, \code{name}, and \code{title} identify the problem;
\item \code{n_kind} is either \code{"fixed"} or \code{"variable"};
\item \code{n_default} is the default parameter dimension;
\item \code{m_configurable} reports whether \code{m} is a factory argument; and
\item \code{m_default} is the effective \code{m} at \code{n_default}.
}

\code{funconstrain_problem()} returns a plain list with \code{fn}, \code{gr}, \code{he}, and
\code{fg} callbacks; a concrete numeric \code{x0}; and \code{problem}, \code{configuration},
and \code{reference} metadata. The callbacks accept only vectors whose length
equals \code{configuration$effective$n}.

\code{configuration} records normalized explicit requests under \code{requested}
(omitted values remain \code{NULL}) and concrete integer values under
\code{effective}.

Each \code{fmin} and \code{xmin} reference record contains \code{value}, \code{status},
\code{reason}, and \code{source_configuration}. Status is one of \code{"applicable"},
\code{"not_applicable"}, \code{"unavailable"}, or \code{"unknown"}. A stored value is
returned even when it is not applicable so that its documented source
configuration remains inspectable; unavailable values are \code{NULL}.
Reasons currently distinguish documented applicability, a source
configuration mismatch, no single stored minimizer, and an unencoded
applicability rule. \code{source_configuration} is a partial rule: \code{n} and \code{m}
are exact values, \verb{_min} and \verb{_max} suffixes are inclusive bounds, and an
omitted component applies to every package-supported value.
}
\description{
\code{funconstrain_catalog()} lists the 35 Moré--Garbow--Hillstrom test problems
in paper order. \code{funconstrain_problem()} resolves one exact factory name and
an optional dimension request to a concrete, solver-neutral problem.
}
\examples{
catalog <- funconstrain_catalog()
catalog[c(1, 20, 35), ]

fixed <- funconstrain_problem("rosen")
fixed$fn(fixed$x0)

variable_n <- funconstrain_problem("chebyquad", n = 10)
length(variable_n$x0)

configurable_m <- funconstrain_problem("jenn_samp", m = 20)
configurable_m$configuration$effective

different_references <- funconstrain_problem("ex_rosen", n = 4)
different_references$reference$fmin$status
different_references$reference$xmin$status

}
\references{
Moré, J. J., Garbow, B. S., & Hillstrom, K. E. (1981).
Testing unconstrained optimization software.
\emph{ACM Transactions on Mathematical Software (TOMS)}, \emph{7}(1), 17-41.
\url{https://doi.org/10.1145/355934.355936}
}
